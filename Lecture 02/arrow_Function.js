const a = 5
const b = 10

const add = function(x, y) {
    return x + y;
}

const addarrow = (x, y) => {
    return x + y;
}

const subtract = (x, y) => {
    const result = a - b;
    return result;
}

console.log(add(a, b));
console.log(addarrow(a, b));
console.log(subtract(a, b));